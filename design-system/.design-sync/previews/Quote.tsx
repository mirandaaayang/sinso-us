import React from 'react';
import { Quote } from '@sinso/design-system';

export const Default = () => (
  <div style={{ maxWidth: 600 }}>
    <Quote
      quote="The deep blues and vivid oranges in our Holton Rower collaboration came out exactly as the artist intended — we had tried two other printers first."
      cite="— InJoy Puzzle Company, Brooklyn NY"
    />
  </div>
);
