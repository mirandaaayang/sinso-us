import React from 'react';
import { Container } from '@sinso/design-system';

export const Default = () => (
  <Container>
    <h2 style={{ fontFamily: 'var(--font-head)', margin: 0 }}>Three verticals. One ECG press.</h2>
    <p style={{ color: 'var(--text-2)', marginTop: 12 }}>
      Artist and collector editions, games and puzzles, and retail packaging — three verticals where print
      quality is product quality, not a cost to minimize.
    </p>
  </Container>
);
